<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.unshareable"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="me.unsharable.activities.FacebookLoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="me.unsharable.activities.FullscreenQuestionResponseActivity"></activity>
        <activity android:name="me.unsharable.activities.AskQuestionActivity"></activity>
        <activity android:name="me.unsharable.activities.QuestionResponsesActivity"></activity>
        <activity android:name="me.unsharable.activities.CategoryListActivity"></activity>
        <activity android:name="me.unsharable.activities.DashboardActivity"></activity>
        <activity android:name="me.unsharable.activities.FacebookLoginActivity"></activity>
        </application>

</manifest>
